const Dashboard = () => {
  return (
    <div>
      <h1>dashboard</h1>
      <form action="">
        {/* Todo
            - add inputs.
            - add post button
            - hook firestore and upload post
            - show it on homepage
        */}
      </form>
    </div>
  );
};

export default Dashboard;
